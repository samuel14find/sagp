<!--Esse Partial View vai nos permitir verificar se o usuário está logado ou não
Se ele estiver logado vai mostrar um ling com o nome do usuário e a opção de logount
Se ele não estiver logado vai ser mostrado dois links para se registrar ou fazer
login. Isso tudo é gerenciado pelo SignInManager.IsSigned -->
@using Microsoft.AspNetCore.Identity

<!--Aqui temos um injeção de dependência -->
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


@* @if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="nav-link btn btn-link text-dark">Logout</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
} *@

     @if (SignInManager.IsSignedIn(User))
     {
        <a id="navbarDropdown" class="nav-link text-light dropdown-toggle" role="button"  data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage"><i class="fa fa-user-circle" aria-hidden="true"></i>Olá @UserManager.GetUserName(User)!</a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            @* <a class="dropdown-item" href="settings.html"><i class="fa fa-cog" aria-hidden="true"></i> Settings</a> *@
            @* <div class="dropdown-divider"></div>
            <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", new { area = "" })" ><i class="fa fa-sign-out" aria-hidden="true"></i> Logout</a> *@
            <form id="logoutForm" class="" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", new { area = "" })">
            @* <button id="logout" type="submit" class="nav-link text-light"></button> *@
            <button class="btn btn-red btn-sm" type="submit">Logout</button>
            </form>
        </div>
     }
     else
     {
         @* <a class="nav-link text-light" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
         <a class="nav-link text-light" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a> *@
         <a id="navbarDropdown" class="nav-link dropdown-toggle text-light" role="button"  data-toggle="dropdown" aria-haspopup="true" aria-expanded="false" title="Manage">
         <i class="fa fa-user-circle" aria-hidden="true"></i>Olá Usuário</a>
         <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Register" ><i class="fa fa-cog" aria-hidden="true"></i>Registrar</a>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" asp-area="Identity" asp-page="/Account/Login" ><i class="fa fa-sign-out" aria-hidden="true"></i> Login</a>
         </div>



     }
